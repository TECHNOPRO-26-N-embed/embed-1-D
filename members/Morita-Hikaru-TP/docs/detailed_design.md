# 詳細設計書 — 組込み開発実習

<!-- 作成者: あなたの名前 / 日付: YYYY-MM-DD / グループ: 〇-〇 -->

> **このドキュメントの目的**
> 基本設計書（basic_design.md）で「**どのような構造で作るか**」を決めました。
> この詳細設計書では「**各処理を具体的にどう実装するか**」を決めます。
> 書き終わったとき、**コードの骨格がほぼ完成している**状態を目指してください。

> [!NOTE]
> **V字モデルにおける位置づけ**
> 詳細設計書 ←→ **単体テスト**（関数・部品ごとのテスト）が対応します。
> 「この関数が正しく動くか」の確認は Section 5 の単体テスト仕様書で計画します。
> ※ 必須機能全体が動くかの「結合テスト」は基本設計書（Section 6）に記載します。

---

## 0. 基本設計書との接続確認

| 項目 | basic_design.md から転記 |
|:--|:--|
| 作品タイトル |手などをかざすと光るLEDがつくその後モータを使い風を吹かすシステム |
| 状態の種類（1-2 状態遷移から） |以下の通り|
| 待機中| 超音波センサーを100ms周期で監視 | LED：OFF / モーター：OFF |
| 動作中| 距離が50cm以下→ LED点灯 & モーター回転 | LED：ON / モーター：ON ||
| 実装する関数の数（2-2 関数一覧から） | 9　個 |
| グローバル変数の合計バイト数（2-1 SRAM確認から） | 18　B |

---

## 1. グローバル変数・定数の設計

> ※ 基本設計書（2-1 データ設計）をもとに、**型と初期値まで**決めます。
> ここで設計した変数は、この後の関数設計でそのまま使います。

```
【ピン定義】
  trigPin    = 9
  echoPin    = 10
  ledPin     = 3
  motorPin   = 5

【状態管理】
  currentState  : int = 0   // 0:待機中 1:動作中

【タイマー（millis()用）】
  lastSensorMillis : unsigned long = 0

【センサー・入力値】
  distance      : int = 0
  lastDistance  : int = 0   // ★追加：AIレビューで追加

【定数】
  SENSOR_INTERVAL : const int = 100

```

---

## 2. 各関数の詳細設計

> ※ 基本設計書（2-2 関数一覧）で定義した各関数の「中身」を設計します。
> **疑似コード**（日本語＋処理の流れ）で書いてください。実際のC++コードは書かなくてOKです。

---

### `setup()` — 初期化処理

```
【処理の流れ】
1. ピンモードを設定する
   - PIN_BUTTON  → INPUT_PULLUP
   - PIN_LED_*   → OUTPUT
   - PIN_BUZZER  → OUTPUT

2. ライブラリの初期化（使うものだけ）
   - 例: lcd.begin(16, 2)
   - 例: servo.attach(PIN_SERVO)

3. Serial.begin(9600)（デバッグ用）

4. 起動確認（任意）: 緑LEDを1秒点灯して消灯
```

**↓ 自分の setup() を設計してください**
```
【処理の流れ】

1. ピンモードを設定する
   - trigPin  → OUTPUT     （超音波センサー TRIG）
   - echoPin  → INPUT      （超音波センサー ECHO）
   - ledPin   → OUTPUT     （LED）
   - motorPin → OUTPUT     （モータードライバ制御）

2. ライブラリの初期化（必要なもののみ）
   - 今回は特になし（LCDやサーボを使用しないため）

3. Serial.begin(9600)（デバッグ用）

4. 起動確認（任意）
   - LED を一旦 LOW（消灯）にする
   - モーターも LOW（停止）にする

| コード | 設計書での表現 |
| --- | --- |
| pinMode(trigPin, OUTPUT) | ピンモード設定（TRIG） |
| pinMode(echoPin, INPUT) | ピンモード設定（ECHO） |
| pinMode(ledPin, OUTPUT) | ピンモード設定（LED） |
| pinMode(motorPin, OUTPUT) | ピンモード設定（モーター） |
| Serial.begin(9600) | デバッグ用シリアル初期化 |
| digitalWrite(ledPin, LOW) | 起動確認（LED消灯） |
| digitalWrite(motorPin, LOW) | 起動確認（モーター停止） |
```

---

### `loop()` — メインループ

> ※ loop() は「状態ごとに何をするか」だけ書く。細かい処理は各関数に任せる。

```
【処理の流れ】

＜毎ループ実行すること＞
  - 入力を読む（readButton(), readSensor() などを呼ぶ）
  - 現在時刻を取得: now = millis()

＜currentState が 0（待機中）のとき＞
  - センサー値を監視する
  - 検知条件を満たしたら → currentState = 1

＜currentState が 1（動作中）のとき＞
  - メイン処理を行う
  - 終了条件を満たしたら → currentState = 2

＜currentState が 2（完了）のとき＞
  - 完了表示をする
  - リセットボタンが押されたら → currentState = 0

＜currentState が 3（エラー）のとき＞
  - エラー表示をする / リセットを待つ

```

**↓ 自分の loop() を設計してください**
```
【処理の流れ】

＜毎ループ実行すること＞
- 現在時刻を取得: now = millis()
- センサー値の更新周期を判定し、必要ならreadSensor()を呼ぶ
- updateState()で状態を更新
- updateOutput()で出力制御

＜currentState が 0（待機中）のとき＞
- LEDとモーターをOFF
- 距離が50cm以下になったら currentState = 1

＜currentState が 1（動作中）のとき＞
- LEDとモーターをON
- 距離が50cmを超えたら currentState = 0

```

---

### （関数ごとに以下のブロックをコピーして追加してください）

> ※ 基本設計書 2-2 の関数一覧に記載した関数を1つずつ設計します。

---

### `関数名()` — （役割を1行で書く）

**basic_design.md 2-2 との対応：** （基本設計書の関数一覧の説明を転記）

**引数：** `引数名`（型）: 何の値か

**戻り値：** 型（なしの場合は void）

```
readSensor() — 超音波距離を測定し distance に格納
引数： なし
戻り値： int（距離[cm]）
【処理の流れ】
1. TRIG ピンを LOW → 2μs待つ → HIGH → 10μs → LOW
2. ECHOピンのHIGH時間をpulseInで取得
3. 距離(cm) = (HIGH時間[μs] / 2) / 29.1
4.異常値処理
   - 距離 == 0 または 距離 >= 400 の場合
       ・初回以外 → lastDistance を使用
       ・初回（lastDistance=0 のとき）→ 0をそのまま返す
       distance が -1 のときは 前回の正常値を使う。
       if (distance < 0 || distance >= 400) {
    distance = lastDistance;   // 安全な前回値を使う
}

正常値なら lastDistance = distance に更新

6. Serial.print(distance)
   ★注意：多用すると処理が重くなるためデバッグ時のみ使用
【エラー・異常ケース】
- 異常な値が来た場合:
TRIG信号異常な場合
①TRIG ピンが LOW のまま、または常に HIGH になっている。
②配線抜け・ショートで正しいパルスが出ない。

ECHO信号異常な場合
①ECHO が HIGH のまま戻らない（センサー故障・配線不良）
②ECHO が全く HIGH にならない（TRIG が届いていない、距離が遠すぎる）

測定値の異常な場合
①0cm（物体が近すぎる・ECHO異常）
②400cm以上（反射が弱い・角度が悪い・対象が吸音材）

環境要因が原因の場合
①温度変化で音速が変わり距離誤差
②風・振動・斜めの面で反射しない
③ノイズ源（別の超音波センサー）が干渉




updateState() — distance に応じて状態を変更
引数： なし
戻り値： なし
【処理の流れ】
0. currentState の破損対策
   if (currentState != 0 && currentState != 1)
       currentState = 0
1. distance <= 50cm なら currentState = 1
2. distance > 50cm なら currentState = 0
if (currentState == 0 && distance <= 50)
    currentState = 1;
distance = -1 のときは 状態遷移を行わない。
if (currentState == 1 && distance >= 60)
    currentState = 0;
    if (distance < 0) {
    return;   // 状態を変えずに終了
}


【エラー・異常ケース】
- 異常な値が来た場合:
distance が異常値な場合
①readSensor() が 0 や 400cm を返し続ける
②前回値保持のロジックが誤動作し、古い値のまま状態が変わらない

currentState の破損の場合
①メモリ破損・未初期化で 0/1 以外の値になると、updateOutput() が想定外の動作をおこす。



updateOutput() — LED とモーターの ON/OFF
引数： なし
戻り値： なし
【処理の流れ】
1. currentState == 1 なら turnOnLED(), motorOn()
2. currentState == 0 なら turnOffLED(), motorOff()
3. currentState が不正値 → 安全側として OFF
updateOutput() は安全側（OFF）に倒す
if (currentState != 0 && currentState != 1) {
    turnOffLED();
    motorOff();
}

【エラー・異常ケース】
- 異常な値が来た場合:
currentState が不正値な場合
0/1 以外 → どちらの処理も実行されず、LED・モーターが停止したまま

ピン設定ミスの場合
pinMode が OUTPUT になっていない場合digitalWrite が効かない

電源不足の場合
①モーター起動時に電圧降下 し、 Arduino リセット
②LED が暗い・モーターが回らない

ドライバ回路の異常の場合
①トランジスタ・MOSFET が壊れて常に ON
②フライバックダイオードなしで逆起電力により破損


turnOnLED() — LED を ON
引数： なし
戻り値： なし
【処理の流れ】
1. digitalWrite(ledPin, HIGH)
【エラー・異常ケース】
- 異常な値が来た場合:
LED の極性逆の場合
点灯しない

抵抗値不適切の場合
①抵抗なし → LED が焼損
②抵抗が大きすぎる → 暗すぎる

ピン故障の場合
HIGH にしても電圧が出ない


turnOffLED() — LED を OFF
引数： なし
戻り値： なし
【処理の流れ】
1. digitalWrite(ledPin, LOW)
【エラー・異常ケース】
- 異常な値が来た場合:
LED の極性逆の場合
点灯しない

抵抗値不適切の場合
①抵抗なし → LED が焼損
②抵抗が大きすぎる → 暗すぎる

ピン故障の場合
HIGH にしても電圧が出ない


motorOn() — モーター回転
引数： なし
戻り値： なし
【処理の流れ】
motorOn():
  1. 10ms の待機（突入電流対策）
  2. digitalWrite(motorPin, HIGH)
1. digitalWrite(motorPin, HIGH)
【エラー・異常ケース】
- 異常な値が来た場合:
モーター直結による過電流の場合
①Arduino ピンが焼損
②電流不足でモーターが動かない

ドライバ回路の誤配線の場合
①モーターが逆回転
②常に ON のまま

逆起電力対策なしの場合
Arduino やトランジスタが破損

電源容量不足
①起動時に電圧降下 → Arduino リセット
②モーターが弱く回る

motorOff() — モーター停止
引数： なし
戻り値： なし
【処理の流れ】1. digitalWrite(motorPin, LOW)
【エラー・異常ケース】
- 異常な値が来た場合:
モーター直結による過電流の場合
①Arduino ピンが焼損
②電流不足でモーターが動かない

ドライバ回路の誤配線の場合
①モーターが逆回転
②常に ON のまま

逆起電力対策なしの場合
Arduino やトランジスタが破損

電源容量不足
①起動時に電圧降下 → Arduino リセット
②モーターが弱く回る
```

---

## 3. 重要ロジックの詳細設計

### 3-1. チャタリング防止（デバウンス処理）

> ※ ボタンを使う場合は必ず設計してください。

```
【考え方】
  ボタンが押されたとき、50ms 以内の連続入力は「同じ1回の押下」として無視する。

【処理の流れ】
  1. ボタンのデジタル値を読む（digitalRead）
  2. 前回確定した時刻（lastDebounceTime）からの経過時間を計算する
  3. 経過時間 < DEBOUNCE_DELAY（例: 50ms）→ 無視する
  4. 経過時間 ≥ DEBOUNCE_DELAY → ボタンの状態として確定する
  5. lastDebounceTime を更新する

【必要な変数（Section 1 に追加済みか確認）】
  lastDebounceTime : unsigned long   // 前回確定した時刻
  DEBOUNCE_DELAY   : const int = 50  // チャタリング判定時間（ms）


  ボタンは使用しない
```

---

### 3-2. millis() を使ったタイマー管理

```
【考え方】
  「前回実行した時刻」を記録しておき、「今の時刻 − 前回時刻 ≥ 周期」なら実行する。

【処理の流れ（例: LED点滅）】
  1. now = millis()
  2. now - lastMillis_LED >= LED_INTERVAL かどうか確認
  3. 条件を満たした場合: LEDのON/OFFを切り替え、lastMillis_LED = now
  4. 条件を満たさない場合: 何もしない（次のループで再チェック）

【自分のシステムで millis() を使う処理】
  （basic_design.md 2-3 のタイミング設計から転記して具体化する）

  1. now = millis() を取得
2. now - lastSensorMillis >= SENSOR_INTERVAL なら
      ・readSensor() を実行して distance を更新
      ・lastSensorMillis = now に更新
3. 条件を満たさない場合は何もしない（次のループで再チェック）

```

---

### 3-3. その他の重要ロジック（任意）

> **【任意】** 複雑なロジックがある場合のみ記入してください。
> 例：「距離に応じたLED点灯パターン」「ゲームの衝突判定」「温度の閾値判定」

```
【処理の流れ】
1.
2.
3.

【入力値と出力値の関係】

```

---

## 4. デバッグ出力計画（任意）

> **【任意】** 関数設計（Section 2）と並行して記入すると効果的です。
> 「動かない」ときに何を確認すればいいかを事前に計画しておきます。
> 実装後は不要な Serial.println() を削除すること。

| No | 確認したい内容 | 挿入する関数 | Serial.println の内容例 |
|:---|:---|:---|:---|
| 1 | センサー値が正しく取れているか | `readSensor()` | `Serial.println(sensorValue);` |
| 2 | 状態遷移が正しく起きているか | `loop()` | `Serial.println(currentState);` |
★注意：Serial.print の多用は処理を遅延させる
- デバッグ時のみ有効化
- 最終版では不要な出力を削除する


---

## 5. 単体テスト仕様書（V字モデル：詳細設計 ↔ 単体テスト）

> ※ 各関数・部品が「単体で正しく動くか」を確認するテスト項目を設計します。
> 「実際の結果」欄は実装後に記入します。

### 5-1. 入力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | readSensor() | センサーを正常範囲で使う | 仕様範囲内の値が返る | | [ ] |
| 2 | readSensor() | センサーを遮蔽・範囲外に向ける | 誤動作しない | | [ ] |
AIレビュー後
| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
| --- | --- | --- | --- | --- | --- |
| 1 | readSensor() | センサーを正常範囲で使う | 仕様範囲内の値が返る |  | [ ] |
| 2 | readSensor() | センサーを遮蔽・範囲外に向ける | 誤動作しない（前回値保持） |  | [ ] |
| 3 | readSensor() | 距離＝50cm（境界値）に物体を置く | 50cm が正しく返る |  | [ ] |
| 4 | readSensor() | 距離＝49cm（閾値内）に物体を置く | 49cm が正しく返る |  | [ ] |
| 5 | readSensor() | 距離＝51cm（閾値外）に物体を置く | 51cm が正しく返る |  | [ ] |
| 6 | readSensor() | pulseIn() が 0 を返す状況を作る | 前回値が保持される |  | [ ] |
| 7 | readSensor() | pulseIn() がタイムアウトする状況を作る | 前回値が保持される |  | [ ] |
| 8 | readSensor() | 初回測定で異常値（0cm）を返す | 初回値が正しく扱われる（0固定にならない） |  | [ ] |

### 5-2. 出力系テスト

| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | updateOutput(0) | state=0（待機中）を渡す | LEDとモーターがOFF | | [ ] |
| 2 | updateOutput(1) | state=1（動作中）を渡す | LEDとモーターがON| | [ ] |

AIレビュー後
| No | テスト対象の関数 | 入力・操作 | 期待する結果 | 実際の結果 | 合否 |
| --- | --- | --- | --- | --- | --- |
| 1 | updateOutput(0) | state=0（待機中）を渡す | LEDとモーターがOFF |  | [ ] |
| 2 | updateOutput(1) | state=1（動作中）を渡す | LEDとモーターがON |  | [ ] |
| 3 | updateOutput(2) | state=2（不正値）を渡す | 安全側としてLED/Motor OFF |  | [ ] |
| 4 | updateOutput(-1) | state=-1（不正値）を渡す | 安全側としてLED/Motor OFF |  | [ ] |

### 5-3. タイミング・並行動作テスト

| No | テスト内容 | テスト手順 | 期待する結果 | 実際の結果 | 合否 |
|:---|:---|:---|:---|:---|:---|
| 1 | millis()タイマーの周期精度 | 点滅をストップウォッチで確認 | 設計した周期（例:500ms）通りに点滅 | | [ ] |

AIレビュー後
| No | テスト内容 | テスト手順 | 期待する結果 | 実際の結果 | 合否 |
| --- | --- | --- | --- | --- | --- |
| 1 | millis()タイマーの周期精度 | センサー読み取り周期を測定 | 100ms周期で readSensor() が呼ばれる |  | [ ] |
| 2 | millis() のオーバーフロー耐性 | millis() を強制的に大きくして動作確認 | オーバーフロー後も周期動作が継続 |  | [ ] |
| 3 | 並行動作確認 | センサー読み取り中に LED/Motor が動作するか確認 | 並行して正しく動作する |  | [ ] |

---

## 6. AIレビュー記録

> グループレビューの前に必ず実施してください。

### Q1: 実装上の問題確認

> 「この詳細設計書に書いた関数と処理フローをもとに Arduino でコードを書きます。バグになりやすい箇所・処理の抜け・型の問題はありますか？」

**AIの回答（要約）：**
| 項目 | 問題点 |
| --- | --- |
| pulseIn() が 0 のときの初回処理 | 初期値 0 のまま固定される |
| currentState の破損対策 | loop() に未記載 |
| TRIG パルス生成忘れ | 距離が常に 0 になる |
| 前回距離保持の変数が未定義 | 設計と実装が不一致 |
| updateOutput と loop の二重管理 | 出力が不安定になる |
| Serial.print の多用 | 処理が重くなる |
| モーター直結の危険 | Arduino が壊れる |

**対応した内容：**

| No | 修正内容（問題点） | 追記した場所 |
| --- | --- | --- | 
| 1 | pulseIn() が 0 の初回処理問題 | **Section 2 → readSensor()（異常値処理）** | 
| 2 | currentState の破損対策 | **Section 2 → updateState() の先頭** | 
| 3 | TRIG パルス生成忘れ | **Section 2 → readSensor() の処理の流れ** | 
| 4 | 前回距離保持の変数が未定義 | **Section 1 → グローバル変数に lastDistance を追加** | 
| 5 | updateOutput と loop の二重管理 | **Section 2 → loop() の説明に追記** | 
| 6 | Serial.print 多用による遅延 | **Section 4 → デバッグ出力計画に注意書き追加** | Serial.print は処理をブロックするため、最終版で削除する必要がある |


---

### Q2: 単体テスト仕様の確認

> 「Section 5 の単体テスト仕様書で、各関数の動作が正しく検証できていますか？テストが不足している項目や、境界値テストが必要な箇所を教えてください。」

**AIの回答（要約）：**
不足一覧
| No | 条件 | 期待結果 |
| --- | --- | --- |
| 1 | 待機中で distance=49 | 動作中へ遷移 |
| 2 | 動作中で distance=51 | 待機中へ遷移 |
| 3 | 動作中で distance=0（異常値） | 状態が変わらない |
| 4 | currentState=2（破損） | 0 に戻る |

**対応した内容：**
それぞれの不足分の追記した。詳しくはAIレビュー後に記載。

---

## 7. グループレビュー記録

### 7-1. 指摘一覧

| No | 指摘内容 | 指摘者 | 対応 |
|:---|:---|:---|:---|
| 1 |　電流不足でリセットするケースがあるとありましたが、対策やコードの修正はありますか  | リクさん |motorOn() の改善、updateState() にヒステリシスを追加  |
| 2 | 前回基本設計から追加した要素、変えた要素 | 結城さん |追加した要素はゼロ細かくした。  |
| 3 | コーディング作業に移りやすい詳細設計書だと思いました。 |  田原さん| 対応なし |
| 4 |途中で抵抗が大きすぎるというのがあったと思うんですが、大きすぎるってどれくらいですか|和田さん|150〜330Ωくらいが使用範囲これをこえると大きい|

### 7-2. レビューを受けて変更した点

-motorOn() の改善、updateState() にヒステリシスを追加 コードを追加した。
--1 のときの処理についても記載する。

---

*初版: 2026-05-25 / AIレビュー: 2026-05-25 / グループレビュー後更新: 2026-05-25*
